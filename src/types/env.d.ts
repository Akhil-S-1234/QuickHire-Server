// // src/types/env.d.ts

// declare global {
//     namespace NodeJS {
//       interface ProcessEnv {
//         PORT: string; // Port should be a string (it’s usually a string, even if it’s a number)
//         MONGO_URI: string; // MongoDB connection URI
//         EMAIL_USER: string; // Email user (e.g., Gmail account)
//         EMAIL_PASS: string; // Email password (preferably stored securely)
//         SESSION_SECRET: string; // Session secret for signing session IDs
//         JWT_ACCESS_SECRET: string; // JWT secret for access tokens
//         JWT_REFRESH_SECRET: string; // JWT secret for refresh tokens
//       }
//     }
//   }
  
//   // This ensures the file is treated as a module
//   export {};
  